# CLAUDE.md

## シェルスクリプト

- 特別な理由がない限り、すべての文字列は引用符で囲む。
- string interpolation が必要な場合は `"foo${VAR}bar"` のように二重引用符を使う。
- string interpolation が不要な場合は `'foobar'` のように単一引用符を使う。
- このルールに従えない場合、その理由をコメントで付記する。
